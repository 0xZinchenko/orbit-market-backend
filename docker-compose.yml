services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: cosmocats
      POSTGRES_USER: cosmocats
      POSTGRES_PASSWORD: cosmocats
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
